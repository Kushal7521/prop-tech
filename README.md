# Property Tech Web Application

This is a full-stack application for automating architectural design.

## Project Structure

- `backend/` - FastAPI Python backend
- `frontend/` - React TypeScript frontend with Vite

## Setup

### Backend

1. Navigate to `backend/` directory
2. Create virtual environment: `python -m venv venv`
3. Activate: `venv\Scripts\activate` (Windows)
4. Install dependencies: `pip install -r requirements.txt`
5. Run: `uvicorn main:app --reload`

### Frontend

1. Navigate to `frontend/` directory
2. Install dependencies: `npm install`
3. Run: `npm run dev`

## Phases

- Phase 1: Project Setup & Scaffolding ✅
- Phase 2: Parametric 3D Generator (Backend)
- Phase 3: 3D Web Viewer (Frontend)
- Phase 4: AI Photorealistic Rendering Pipeline